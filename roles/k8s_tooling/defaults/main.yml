---
# Only used when k8s_control is the node itself (AWS/SSM). On a workstation you
# already have these.
k8s_tooling_helm_version: v3.16.3
k8s_tooling_pip_break_system_packages: true   # PEP668 distros (AL2023, Ubuntu 24)
