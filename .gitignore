# Real secrets and environment-specific config: templates are committed instead.
group_vars/all/secrets.yml
inventory/hosts.yml
.vault_pass

# Fetched from the node at runtime
.kubeconfig/

*.retry
*.tar.gz
