---
# SINGLE SOURCE OF TRUTH for the lab node's address.
# group_vars derives node_ip from this; nothing else hardcodes an IP.
all:
  children:
    lab_node:
      hosts:
        byocallinone:
          ansible_host: 192.168.220.72
          ansible_user: root
